language: android

android:
  components:
    - platform-tools
    - tools
    - android-28

before_install:
  - yes | sdkmanager "platforms;android-28"
  - yes | sdkmanager "build-tools;28.0.3"

script: ./gradlew build check
